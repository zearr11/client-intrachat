export interface OptionsPaginated {
  page?: number,
  size?: number,
  estado: boolean,
  filtro?: string
}

export interface OptionsPaginatedMessage {
  page?: number,
  size?: number,
  filtro?: string
}
